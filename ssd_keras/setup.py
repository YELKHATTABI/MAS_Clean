from setuptools import setup, find_packages

setup(
    name = "ssd_keras",
    version = "0.0.1",
    description = "ssd_keras_library",
    py_module = ["ssd_keras"],
    packages = find_packages(),
)